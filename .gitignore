node_modules/
package-lock.json
.DS_Store

# generated LLM reports (work products, not source)
llm-report*.md

# press drafts — kept private (pitching to outlets; not for public repo)
press/

# stray dose-response outputs written to the repo root by --out
/flexibility-*.json
/flexibility-*.svg

# skill test workspace (local eval runs)
/personalaity-skill-workspace/
